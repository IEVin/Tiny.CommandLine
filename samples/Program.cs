
namespace System.TinyCommandLine.Samples
{
    class Program
    {
        static void Main(string[] args)
        {
            // List of some useful samples:
            SampleShortForm.EntryPoint(args);
            SampleWithStruct.EntryPoint(args);
            SampleMultiCommands.EntryPoint(args);
        }
    }
}